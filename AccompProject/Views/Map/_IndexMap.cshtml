@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>
<div ng-app="myApp" ng-controller="mapController">
    @*<div class="locations">
        <ul>
            <li ng-repeat="l in locations" ng-click="ShowLocation(l.Row)">{{l.SYSTEMS}}</li>
        </ul>
    </div>*@
    <div class="maps">
        <!-- Add directive code (gmap directive) for show map and markers-->
        <ui-gmap-google-map center="map.center" zoom="map.zoom">
            <ui-gmap-marker ng-repeat="marker in markers" coords="marker.coords" options="marker.options" events="marker.events" idkey="marker.id">
                <ui-gmap-window options="windowOptions" show="windowOptions.show">
                    <div style="max-width:200px">
                        <div class="header"><strong>{{marker.name}}</strong></div>
                        <div id="mapcontent">
                            <p>

                                <div>Municipality : {{marker.municipality}}</div>
                                <div>Service Area : {{marker.sa}}</div>
                                <div>Firmed Up Service Area : {{marker.fusa}}</div>
                                <img ng-src="{{marker.image}}" style="width:200px; height:100px" />
                                

                            </p>
                        </div>
                    </div>
                </ui-gmap-window>
            </ui-gmap-marker>
        </ui-gmap-google-map>
    </div>o
</div>

@* Now here we need to some css and js *@
<style>
    .angular-google-map-container {
        height: 500px;
    }

    .angular-google-map {
        border: #555555 2px solid;
        position: relative;
        height: 490px;
    }

    .locations {
        width: 200px;
        float: left;
    }

        .locations ul {
            padding: 0px;
            margin: 0px;
            margin-right: 20px;
        }

            .locations ul li {
                list-style-type: none;
                padding: 5px;
                border-bottom: 1px solid #f3f3f3;
                cursor: pointer;
            }
</style>
