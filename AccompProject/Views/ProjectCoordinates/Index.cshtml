@model IEnumerable<AccompProject.Models.ProjectCoordinatesView>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.lati)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.longi)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Description)
        </th>
     
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.lati)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.longi)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Description)
        </td>
     
        <td>
            
            @Html.ActionLink("View in Map", "IndexPerProj", "Map", new { id = item.idcoordinates },null) |
            @Html.ActionLink("Edit", "Edit", new { id=item.idcoordinates }) |
        
            @Html.ActionLink("Delete", "Delete", new { id=item.idcoordinates })
        </td>
    </tr>
}

</table>
